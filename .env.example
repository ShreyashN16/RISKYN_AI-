# RISKYN AI runs fully self-contained on synthetic data — no external
# API keys are required for the demo. This file is a placeholder for future
# config if you wire it up to a real transaction feed or processor sandbox.

PORT=8000
